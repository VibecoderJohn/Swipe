services:
  - type: web
    name: biosecure-pay-backend
    env: python
    repo: https://github.com/yourusername/biosecure-pay-mvp
    branch: main
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT app:app
    envVars:
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET_KEY
        generateValue: true